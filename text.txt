Collections in Java is a single / framework single that provides an architecture /to store and manipulate the group of objects.All the  single operations that you perform on a data such as searching, sorting, insertion, manipulation, deletion etc. can be performed by Java Collections.Java/Collection simply single means a single unit of objects. Java Collection framework provides many interfaces (Set, List, Queue, Deque etc.) and classes (ArrayList, Vector, LinkedList, PriorityQueue, HashSet, LinkedHashSet, TreeSet etc).